- yum:
    curl: {}
    wget: {}
    zsh: {}
    libevent-devel: {}
    ncurses-devel: {}
    automake: {}

- shell:
    - command: cd ~ && git clone https://github.com/tmux/tmux.git && cd tmux && git checkout 3.2 && sh autogen.sh && ./configure && make # && sudo make install
      description: Install tmux 3.2
    - rm -rf ~/tmux
