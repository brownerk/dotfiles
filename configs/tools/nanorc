# Settings for `nano` editor
# Full list of settings might be shown via `man nano`
# or here: https://www.nano-editor.org/dist/v6/nanorc.5.html

# Editor:
set tabstospaces
set trimblanks

set softwrap
set atblanks
set constantshow
set linenumbers

# Extra files:
set backup
set backupdir "~/.nano-backups"
set historylog

# line numbers are copied with text,
# it is not useful:
# set linenumbers
